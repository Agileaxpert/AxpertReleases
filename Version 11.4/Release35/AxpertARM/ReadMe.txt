Following issue is fixed in this release:
1) TASK-3320 - Bafco - Menu structure is not the same for a particular user in Mobile and web login.

Note:
1) Please apply on top of existing ARM.
2) Existing config files like appsettings.json, appsettings.ini, service_key.json has to the existing ones.